__pycache__/
.venv/
*.pyc
.env

# DeepEval local cache
.deepeval/

# Regeneratable pipeline outputs (deterministic, no LLM — reproduce via scripts/)
data/knowva_manuals/preprocessed/
data/knowva_manuals/chunks/

# Archived/legacy markdown output — no longer on critical path
data/knowva_manuals/parsed/

# Eval artifacts — large + ephemeral. Reproducible via run_eval.py + score_eval.py
data/eval_*.json
data/golden_set_audit.json

# K-sweep diagnostic outputs — reproducible via scripts/run_eval.py
data/ksweep_*.raw.json
data/ksweep_logs/
